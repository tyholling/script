- name: install common tools
  hosts: centos
  tasks:
  - name: install common tools
    dnf:
      name:
      - git
      - jq
      - podman
      - tmux
      - vim

- name: install common tools
  hosts: debian
  tasks:
  - apt:
      name:
      - ack
      - bat
      - btop
      - git
      - jq
      - kubernetes-client
      - podman
      - tmux
      - vim

- name: install common tools
  hosts: fedora
  tasks:
  - name: install common tools
    dnf:
      name:
      - ack
      - bat
      - btop
      - git
      - git-delta
      - helm
      - jq
      - kubernetes-client
      - podman
      - tmux
      - vim

- name: install common tools
  hosts: ubuntu
  tasks:
  - apt:
      name:
      - ack
      - bat
      - btop
      - git
      - jq
      - podman
      - tmux
      - vim
