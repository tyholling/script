- name: update
  hosts: debian
  tasks:
  - name: update
    apt:
      name: "*"
      state: latest

- name: update
  hosts: fedora
  tasks:
  - name: update
    dnf:
      name: "*"
      state: latest
